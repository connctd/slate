general:
  branches:
    ignore:
    - gh-pages

test:
  pre:
  - git config --global user.email "info@connctd.com"
  - git config --global user.name "CircleCI"
  override:
  - exit 0

deployment:
  production:
    branch: master
    commands:
    - ./deploy.sh
